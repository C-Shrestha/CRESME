@model CRESME.Data.ApplicationUser

@{
    ViewData["Title"] = "Delete by Term(Instructor)";
}

<h1>Delete by Term (Instructor)</h1>


<hr />


<div class="row">
    <div class="col-md-4">
        <form asp-controller="Admin" asp-action="DeleteByTermInstructors" method="post" onsubmit="return confirm('Are you sure you want to delete all instructors in this term?');">

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label>Instructors's Term</label>
                <input asp-for="Term" class="form-control" />
                <span asp-validation-for="Term" class="text-danger"></span>
            </div>

            <br />

            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>

            <br />

        </form>
    </div>
</div>

<div>
    <a asp-action="ListUsers">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
